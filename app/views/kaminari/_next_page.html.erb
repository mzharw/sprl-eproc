<div>
  <%= link_to url, rel: 'next', remote: remote, class: "page-link #{'bg-light disabled' if current_page.last?}", disabled: current_page.first? ? true : false do %>
    <i class="ti ti-chevron-right"></i>
  <% end %>
</div>
