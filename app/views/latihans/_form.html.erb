<%= form_with(model: latihan) do |form| %>
  <% if latihan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(latihan.errors.count, "error") %> prohibited this latihan from being saved:</h2>

      <ul>
        <% latihan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
